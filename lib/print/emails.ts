// Order emails (proof ready, shipped) now live in the central, editable email
// registry at lib/lifecycle.ts (kinds "proof_ready" and "shipped"), so all
// automated email copy is managed in one place and editable from the admin.
// This file is intentionally left as a pointer to avoid duplicate templates.

export {};
